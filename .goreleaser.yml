# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: openframe-cli

before:
  hooks:
    - go mod tidy
    - go mod download

builds:
  - id: openframe-cli
    main: ./main.go
    binary: openframe
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/gippsweb/openframe-cli-dev/cmd.DefaultVersionInfo.Version={{.Version}}
      - -X github.com/gippsweb/openframe-cli-dev/cmd.DefaultVersionInfo.Commit={{.Commit}}
      - -X github.com/gippsweb/openframe-cli-dev/cmd.DefaultVersionInfo.Date={{.Date}}

archives:
  - id: openframe-cli-archive
    ids:
      - openframe-cli
    name_template: "openframe-cli_{{ .Os }}_{{ .Arch }}"
    formats: ["tar.gz"]
    format_overrides:
      - goos: windows
        formats: ["zip"]
    files:
      - README.md
    wrap_in_directory: false

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

changelog:
  use: github-native
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^ci:'
      - '^build:'
      - '^style:'
      - '^refactor:'
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: Bug fixes
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: Others
      order: 999

release:
  make_latest: true
  draft: false
  prerelease: auto
  name_template: "🦩 {{ .Tag }}"
